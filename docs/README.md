# Documents

- `analytic-many-closed-channels.{tex,pdf}` — an earlier, **separate** manuscript,
  *"Analytic scattering model with many closed channels"*, distinct from the published
  *"Chaotic scattering with coupled square wells"* (Phys. Rev. A 98, 062703) in
  `../plots/N-Channel-Square-Well-3/main.tex` (744 lines differ). Was `Nchannel/*/NOTES/paper.tex`,
  identical in all three copies.
- `MoreNChannelNotes.docx` — notes from `Nchannel/NChannel-Inelastic/`, the only content of that
  directory. Relevant to the multi-open-channel generalization tracked in the `Erbium` repo.

Recovered 2026-09-22 from `~/Documents/Code/Nchannel/` before that tree was deleted.
